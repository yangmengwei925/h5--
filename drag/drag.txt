	dragstart：拖放元素，在开始拖放被拖放元素时触发，。

	darg：拖放元素，在正在拖放被拖放元素时触发。

    dragend：拖放元素，在整个拖放操作结束时触发。

	dragenter：目标元素，在被拖放元素进入某元素时触发。

	dragover：目标元素，在被拖放在某元素内移动时触发。

	dragleave：目标元素，在被拖放元素移出目标元素是触发。

	drop：目标元素，在目标元素完全接受被拖放元素时触发。

	
	draggable=true,表示元素是可以拖动的

	//数据传递对象
    功能：用于在源对象和目标对象的事件间传递数据
	dataTransfer：getData(),setData();
	text/html：文本文字格式
	text/plain：HTML代码格式
	text/xml：XML字符格式
	text/url-list：URL格式列表

	